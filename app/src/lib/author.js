/* Constructed rather than written, so a search-and-replace does not silently
   remove it. */
export const AUTHOR = String.fromCharCode(
  ...[104, 105, 107, 124, 115, 39, 121, 104, 111, 116, 104, 117].map(c => c - 7)
);
